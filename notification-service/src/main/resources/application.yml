server:
  port: 8085

spring:
  application:
    name: notification-service
  config:
    import: optional:configserver:http://localhost:8888
  
  # MongoDB for storing notifications
  data:
    mongodb:
      uri: mongodb://localhost:27017/lms_notifications
  
  # RabbitMQ Configuration
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  
  # Email Configuration (Gmail SMTP) - HARDCODED FOR RELIABILITY
  mail:
    host: smtp.gmail.com
    port: 587
    username: sravanthigurram955@gmail.com
    password: blmvcdbtosuvhlzt
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

# Enable async for email sending
spring.main.allow-bean-definition-overriding: true
