spring.cloud.config.enabled=false
spring.cloud.discovery.enabled=false
eureka.client.enabled=false

# Disable RabbitMQ for tests
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
spring.rabbitmq.listener.simple.auto-startup=false

# MongoDB Test Config (Embedded will listen on dynamic port usually, but we can fix it or let boot handle it)
# By default, adding flapdoodle and removing explicit URI makes Boot use embedded.
