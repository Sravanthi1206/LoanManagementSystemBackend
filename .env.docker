# Docker-specific environment (used by docker-compose.yml)
# MySQL requires non-root user for MYSQL_USER

# MySQL Database
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=root
MYSQL_USER=lms_user
MYSQL_PASSWORD=lms_password

# MongoDB (for notification-service)
MONGODB_USER=mongo
MONGODB_PASS=mongo

# RabbitMQ
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASS=guest

# JWT Configuration
JWT_SECRET=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
JWT_EXPIRATION=3600000

# Email (SMTP)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=sravanthigurram955@gmail.com
MAIL_PASSWORD=blmvcdbtosuvhlzt

# Eureka Discovery Server
EUREKA_HOST=discovery-server
EUREKA_PORT=8761

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:4200

# Root Admin Seed User
ROOT_ADMIN_EMAIL=root@lms.com
ROOT_ADMIN_PASSWORD=Password@123
